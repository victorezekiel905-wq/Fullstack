// Placeholder for tracing - removed for Render Free compatibility
export const startTracing = () => {
  console.log('⚡ Tracing disabled for Render Free tier optimization');
};

export const shutdownTracing = async () => {
  console.log('⚡ Tracing shutdown (no-op)');
};
